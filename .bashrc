export NODE_OPTIONS="--max-old-space-size=8192"

# Aliases
# alias alias_name="command_to_run"

# Push current branch to origin. Returns a link to create MR.
alias createmr="git cb | xargs git puo"

# Go to angular frontend folder
alias sdfe="cd ~/Work/angular-frontend"

# Start the angular frontend project
alias startfe="npm start"